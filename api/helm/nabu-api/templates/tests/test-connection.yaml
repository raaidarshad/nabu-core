apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nabu-api.fullname" . }}-test-connection"
  labels:
{{ include "nabu-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nabu-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
